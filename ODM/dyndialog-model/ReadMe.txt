This project defines the domain object model for the rule processing.

It includes questionnaire, question, assessment business entity definition.
Each class has JAXB annotation for xml or json serialization.

The ant script compile and build a jar for the model.
